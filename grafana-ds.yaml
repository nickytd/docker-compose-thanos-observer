apiVersion: 1

datasources:
  - name: Prometheus
    default: true
    type: prometheus
    # Access mode - proxy (server in the UI) or direct (browser in the UI).
    access: proxy
    url: http://thanos_query_frontend:9090